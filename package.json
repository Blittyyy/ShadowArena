{
  "name": "vibejam-shadow-arena",
  "private": true,
  "scripts": {
    "start": "serve -l 5173 .",
    "dev": "serve -l 5173 .",
    "build": "echo \"No bundler — run npm ci --omit=dev before deploy so node_modules/three exists.\""
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "serve": "^14.2.4"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
